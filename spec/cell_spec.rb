require 'spec_helper'
  
describe Cell do 
  context "determine state of cell" do
    it "should be dead in next generation if neighbouring cells are dead" do
      cell = Cell.new()
    end
  end
end